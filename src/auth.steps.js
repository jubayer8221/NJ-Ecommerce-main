/**
 * Firebase hosting
 * one time for each computer
 * 1. npm install -g firebase-tools
 * 2. firebase login --reauth
 *
 * for each project one time
 * 3. firebase init
 * make sure: for public directory: you select: build single page application: y
 *
 *
 * for every deploy
 * npm run build
 * firebase deploy
 */
